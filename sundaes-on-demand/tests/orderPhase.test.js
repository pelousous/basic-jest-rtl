import { render, screee } from '@testing-library/react';
import userEvent from '@testing-library/user-event';

import App from '../src/App';

test('order Phase for happy path', () => {
  // render app

  // add ice cream scoops and toppings

  // find and click the order button

  // chec summary infos based on order

  // accept terms and conditions and click button to confirm order

  // confirm order number on confirmation page

  // click "new order" button on confirmation page

  // check that scoops and toppings subtotals have been resetted

  // do we need to await anything to avoid test errors?
});